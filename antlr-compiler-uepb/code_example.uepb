spawn x : int;
spawn str : string = "Aquele seu \"Amigo\" é engraçado";

x = -23*(3+5.2);

unless("x" != 0){
    spawn x : int = x + 2;
    during(x >= 0){
        spawn y : int = x + 3;
        x = x - 1;
        stop;
    };
}do{
    x = x - 1;
};

ask();
show(str);